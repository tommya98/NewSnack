import styled from "@emotion/styled";

function AppTitle() {
  return <Title>Newsnack</Title>;
}

const Title = styled.div`
  font-size: 28px;
  font-weight: 700;
  line-height: 40px;
`;

export default AppTitle;
